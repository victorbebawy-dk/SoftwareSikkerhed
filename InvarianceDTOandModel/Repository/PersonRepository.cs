using ValidateTheNameModelBinding.Models;

namespace ValidateTheNameModelBinding.Util
{
    public class PersonRepository
    {
        public void AddPerson(Person person)
        {
            /// This is intentionally left blank. The purpose
            /// of this exercise is to show that a DTO should not
            /// be passed onwards from the controller to other classes
            ///
            /// This method does not have to perform any checks, as Objects
            /// of type Person those that them self 
        }
    }
}
